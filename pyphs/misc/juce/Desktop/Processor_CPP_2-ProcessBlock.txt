        // Recover left data.        float* leftData = buffer.getWritePointer(0);            // Set right data equal to left data if plugin is mono.        float* rightData = buffer.getWritePointer(0);        if (totalNumInputChannels==2)        {                rightData = buffer.getWritePointer(1);        }            // Walk over buffer elements        for(long i=0; i<buffer.getNumSamples(); i++)        {                // Single input of PHS is average of left and right data.            phsInput = (leftData[i] + rightData[i])/2.f;                        // Update PHS input.            myobject.set_u(phsInput, phsInputIndex);                    // Core PHS update.            myobject.update();                    // Get PHS ouput.            leftData[i] = myobject.y(phsOutputIndex);                    if (totalNumOutputChannels==2)            {                    rightData[i] = myobject.y(phsOutputIndex);            }        }